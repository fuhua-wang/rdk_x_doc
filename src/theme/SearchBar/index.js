import React from "react";

export default function SearchBar() {
  // Algolia integration is temporarily disabled.
  // Keeping this swizzled component to avoid fallback behavior changes.
  return null;
}
